<form method="post" action="{{ path('app_profile_delete', {'username': user.username}) }}" onsubmit="return confirm('Etes-vous sûre de vouloir supprimer votre compte utilisateur ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.username) }}">
	<button class="btn btn-comments btn-delete">Supprimer votre compte</button>
</form>
